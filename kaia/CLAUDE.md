@AGENTS.md

## Hard Override

Follow the Linux CI deployment policy in `AGENTS.md` with no exceptions:
- No local Windows production deploys.
- Use GitHub Actions Linux runners for release build/deploy.
